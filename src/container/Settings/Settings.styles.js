import {StyleSheet} from "react-native";

export default StyleSheet.create({
    version: {
        marginTop: 10,
        marginLeft: 'auto',
        marginRight: 'auto',
        opacity: 0.35
    }
});
