import {StyleSheet} from "react-native";

export default StyleSheet.create({
    taskName: {
        marginLeft: 15,
        fontSize: 18,
        color: "#000"
    },
    taskIconContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        marginRight: 15
    }
});
