import {StyleSheet} from "react-native";

export default StyleSheet.create({
    version: {
        margin: 5,
        marginLeft: 'auto',
        marginRight: 'auto',
        opacity: 0.35
    }
});
