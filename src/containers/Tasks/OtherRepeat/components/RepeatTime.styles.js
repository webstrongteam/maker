import { StyleSheet } from 'react-native'

export default StyleSheet.create({
	buttons: {
		flex: 2,
		flexDirection: 'row',
		alignItems: 'center',
		justifyContent: 'space-between',
		margin: 50,
	},
})
